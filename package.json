{
  "name": "tattoo-sfumato",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests",
    "dev": "vite",
    "build": "node scripts/prepare-index.js && vite build && node scripts/publish-static.js",
    "preview": "vite preview",
    "start": "node app.js",
    "live": "npm install --include=dev --include=prod --no-audit --no-fund && npm run build && node app.js"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  },
  "dependencies": {
    "mailparser": "3.9.15",
    "nodemailer": "^9.0.5",
    "qrcode": "^1.5.4"
  },
  "overrides": {
    "deepmerge-ts": "8.0.2"
  }
}
